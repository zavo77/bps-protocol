{
  "name": "@bps/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Solidity contracts (Foundry project). Requires forge on PATH.",
  "scripts": {
    "forge:build": "forge build",
    "forge:test": "forge test -vv",
    "forge:fmt": "forge fmt --check"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.6.1"
  }
}
